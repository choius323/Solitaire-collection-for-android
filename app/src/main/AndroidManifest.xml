<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.solitairecollection">
    <!-- 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolitaireCollection">
        <activity
            android:name=".ScoreBoardActivity"
            android:exported="false"
            android:label="@string/score_board"
            android:theme="@style/Theme.SolitaireCollection.NoActionBar" />
        <activity
            android:name=".ScorpionActivity"
            android:exported="false"
            android:label="@string/title_activity_scorpion"
            android:theme="@style/Theme.SolitaireCollection.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SolitaireCollection.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>